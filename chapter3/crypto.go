package chapter3

func encrypt(key string, chipherTextRaw string, textToEncrypt string) {
/*	block, err := aes.NewCipher([]byte(key))

	if err != nil {
		panic(err)
	}
	str := []byte(textToEncrypt)*/
}
